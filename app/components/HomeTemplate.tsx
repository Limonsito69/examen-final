import WorkoutList from "./WorkoutList";

const HomeTemplate = () => (
  <main style={{ background: "#121212", minHeight: "100vh", color: "#fff" }}>
    <WorkoutList />
  </main>
);

export default HomeTemplate;
